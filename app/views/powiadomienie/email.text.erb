Dzień dobry

Widma mas dla podanych niżej próbek są gotowe:

Nazwa próbki ---- Rodzaj pomiaru
    <% @zlecenia.each do |order| %>

<%= order.sample_name %> ---- <% ilosc = order.measurements.count %><% i = 0 %><% order.measurements.each do |t| %><% i = i + 1 %><%= t.technique.short_name %> <%= "," unless i == ilosc  %> <% end %>

    <% end %>

Próbki oraz widma w wersji papierowej można odebrać w pokoju 011.
Widma w wersji elektronicznej można znaleźć na serwerze plików naszego laboratorium.

!!! Proszę nie odpowiadać na tego emaila !!!